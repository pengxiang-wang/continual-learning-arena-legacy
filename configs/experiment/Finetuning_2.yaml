# @package _global_

defaults:
  - override /data: til_permuted_cifar10.yaml
  - override /model: finetuning_resnet_til.yaml
  - override /callbacks: default.yaml
  - override /logger: many_loggers.yaml
  - override /trainer: default.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

experiment_name: "Finetuning_1"

tags: ["TIL", "Permuted CIFAR10", "ResNet", "Finetuning"]

seed: 12345

# major settings overwrite here
# refer to the detailed configs for other settings

data:
  batch_size: 128
  num_tasks: 50

model:

  optimizer: 
    lr: 0.001